# Neovim Keybindings

`<leader>` = `Space`

---

## Общие

| Клавиша | Действие |
|---|---|
| `<leader>w` | Сохранить файл |
| `<leader>q` | Закрыть |
| `<leader>a` | Выделить всё |
| `<leader>j` | Добавить строку ниже |
| `<leader>k` | Добавить строку выше |
| `<leader>o` | Новая строка ниже, курсор туда |
| `<leader>O` | Новая строка выше, курсор туда |
| `<leader>;` | Добавить `;` в конец строки |
| `<leader>-` | Добавить пробел после курсора |
| `<leader>_` | Добавить пробел перед курсором |
| `<leader>s` | Source текущего файла |
| `<leader><CR>` | Перенести остаток строки на следующую |
| `<leader><Tab>` | Вставить Tab в normal mode |
| `<leader>t` | Открыть/закрыть терминал (1/3 высоты) |
| `<C-Space>` / `<C-@>` | Открыть/закрыть терминал (n, i, t режимы) |
| `<C-D-Space>` | Открыть поиск символов (Telescope) |
| `<leader>h` | Закрыть другие окна + открыть Vifm |
| `gb` | Назад (jump list) |
| `n` / `N` | Следующий/предыдущий результат поиска (по центру) |
| `*` / `#` | Поиск слова под курсором вперёд/назад (по центру) |
| `g*` / `g#` | Как `*`/`#`, без границ слова |
| `<C-u>` / `<C-d>` | Страница вверх/вниз (по центру) |
| `<C-h/j/k/l>` | Переключение между окнами |
| `<C-S-h/l>` | Уменьшить/увеличить ширину окна |
| `<C-S-j/k>` | Уменьшить/увеличить высоту окна |
| `<M-k>` / `<M-j>` | Переместить строку вверх/вниз |

### Visual mode

| Клавиша | Действие |
|---|---|
| `<M-k>` / `<M-j>` | Переместить выделенные строки вверх/вниз |
| `p` | Вставить без перезаписи регистра |

---

## Telescope

| Клавиша | Действие |
|---|---|
| `<leader>f` | Поиск файлов |
| `<leader>l` | Live grep |
| `<leader>s` | LSP символы документа |
| `<leader>b` | Недавние файлы |
| `<C-b>` | Предыдущий буфер |
| `l` *(в telescope)* | Открыть выбранное |

---

## LSP

| Клавиша | Действие |
|---|---|
| `K` | Hover документация |
| `gd` | Перейти к определению |
| `gr` | Показать ссылки |
| `rn` | Переименовать символ |
| `<leader>ca` | Code action |
| `gl` | Показать диагностику (float) |
| `[d` / `]d` | Предыдущая/следующая диагностика |

---

## Отладка (DAP)

| Клавиша | Действие |
|---|---|
| `<Leader>db` | Поставить/убрать breakpoint |
| `<Leader>dc` | Продолжить |
| `<Leader>dr` | Запустить до курсора |
| `<Leader>di` | Шаг внутрь |
| `<Leader>do` | Шаг через |
| `<Leader>dO` | Шаг из |
| `<Leader>dl` | Повторить последний запуск |
| `<Leader>dw` | Добавить слово под курсором в watches |
| `<Leader>dq` | Завершить отладку |
| `<A-l>` | Шаг внутрь *(альт.)* |
| `<A-j>` | Шаг через *(альт.)* |
| `<A-h>` | Шаг из *(альт.)* |
| `<A-k>` | Повторить последний запуск *(альт.)* |

---

## Completion *(insert/select)*

| Клавиша | Действие |
|---|---|
| `<A-Space>` | Вызвать completion вручную |
| `<A-j>` / `<Down>` | Следующий вариант |
| `<A-k>` / `<Up>` | Предыдущий вариант |
| `<A-l>` | Подтвердить / вызвать completion |
| `<A-h>` | Отменить completion |
| `<Tab>` | Подтвердить / прыжок по снипету |
| `<S-Tab>` | Предыдущий вариант / прыжок назад |
| `<CR>` | Подтвердить |
| `<Esc>` | Закрыть popup, выйти из insert |
| `<C-b>` / `<C-f>` | Прокрутить документацию |
| `<C-l>` | Принять одну строку Copilot |
| `<leader>cp` | Включить/выключить Copilot |

---

## Flash *(прыжки)*

| Клавиша | Режим | Действие |
|---|---|---|
| `s` | n, x, o | Прыжок по символам |
| `S` | n, x, o | Прыжок по treesitter |
| `r` | o | Remote flash |
| `R` | o, x | Treesitter search |
| `<C-s>` | c | Переключить flash search |

---

## Git

| Клавиша | Действие |
|---|---|
| `<leader>gp` | Превью hunk |
| `<leader>gt` | Переключить blame строки |

---

## Neo-tree

| Клавиша | Действие |
|---|---|
| `<leader>n` | Открыть/закрыть файловое дерево |
| `l` *(в дереве)* | Открыть узел |
| `L` *(в дереве)* | Открыть в вертикальном сплите |
| `<A-l>` *(в дереве)* | Открыть в горизонтальном сплите |
| `h` *(в дереве)* | Свернуть узел |

---

## Yazi

| Клавиша | Действие |
|---|---|
| `<leader>-` | Открыть yazi у текущего файла |
| `<leader>cw` | Открыть yazi в рабочей директории |
| `<C-Up>` | Возобновить последнюю сессию yazi |

---

## Hex

| Клавиша | Действие |
|---|---|
| `<leader>hx` | Переключить hex view |
| `<leader>hd` | Hex dump |
| `<leader>ha` | Hex assemble (сохранить) |

---

## Сессии / прочее

| Клавиша | Действие |
|---|---|
| `<leader>z` | Поиск сессий (AutoSession) |
| `<leader>dm` | Скрыть уведомления (Noice) |
| `<leader>p` | Переключить тему (Themery) |
| `<leader>c` | Собрать проект |
| `<leader>r` | Запустить проект |

| `<leader>i` | Переключить inlay hints |

---

## Markdown *(только .md)*

| Клавиша | Действие |
|---|---|
| `<leader>mp` | Toggle превью (Markview) |
| `<leader>ms` | Split режим (редактор + превью) |
| `<leader>mh` | Hybrid режим (рендер везде кроме курсора) |

---

## Rust *(только .rs)*

| Клавиша | Действие |
|---|---|
| `K` | Rust hover actions |
| `<leader>ca` | Rust code action |
| `<leader>rr` | Runnables |
| `<leader>rd` | Debuggables |
| `<leader>rt` | Testables |
| `<leader>rc` | Открыть Cargo.toml |
| `<leader>rp` | Родительский модуль |
| `<leader>ro` | Открыть docs.rs |
| `<leader>re` | Объяснить ошибку |
| `<leader>rD` | Render диагностики |
| `<leader>rm` | Развернуть макрос |
| `<leader>rH` | Переключить inlay hints |
| `<leader>rf` | Форматировать (rustfmt) |
| `J` | Умное объединение строк |

### Cargo.toml

| Клавиша | Действие |
|---|---|
| `<leader>ci` | Инфо о крейте |
| `<leader>cv` | Версии крейта |
| `<leader>cf` | Фичи крейта |
| `<leader>cd` | Зависимости крейта |
| `<leader>cu` | Обновить крейт |
| `<leader>cU` | Обновить все крейты |
| `<leader>cA` | Апгрейд всех до latest |

---

## C *(только .c)*

| Клавиша | Действие |
|---|---|
| `<leader>ch` / `<F4>` | Переключить .h ↔ .c |
| `<leader>ci` | Инфо о символе |
| `<leader>ct` | Иерархия типов |
| `<leader>cH` | Переключить inlay hints |
| `<leader>cf` | Форматировать |
| `<leader>cc` | Синтаксическая проверка (gcc) |

---

## C++ *(только .cpp)*

| Клавиша | Действие |
|---|---|
| `<leader>ch` / `<F4>` | Переключить .h ↔ .cpp |
| `<leader>ci` | Инфо о символе |
| `<leader>ct` | Иерархия типов |
| `<leader>cA` | Показать AST |
| `<leader>cm` | Использование памяти clangd |
| `<leader>cH` | Переключить inlay hints |
| `<leader>cf` | Форматировать |
| `<leader>cc` | Синтаксическая проверка (g++) |

---

## Java *(только .java)*

| Клавиша | Режим | Действие |
|---|---|---|
| `<leader>ev` | n / v | Извлечь переменную |
| `<leader>ec` | n / v | Извлечь константу |
| `<leader>em` | v | Извлечь метод |
| `<leader>ei` | n | Инлайн переменной/метода |
| `<leader>io` | n | Организовать импорты |
| `gI` | n | Перейти к реализациям |
| `gS` | n | Перейти к super |
| `gH` | n | Иерархия типов |
| `<leader>gc` | n | Сгенерировать код |
| `<leader>ut` | n | Запустить ближайший тест |
| `<leader>uT` | n | Запустить все тесты класса |
| `<leader>up` | n | Выбрать тест |
| `<leader>ud` | n | Отладить тест |
| `<leader>pu` | n | Обновить конфиг проекта |
| `<leader>cc` | n | Новый Java class |
| `<leader>ci` | n | Новый Java interface |
| `<leader>ce` | n | Новый Java enum |
| `<leader>cr` | n | Новый Java record |

---

## Neovide *(только GUI)*

| Клавиша | Режим | Действие |
|---|---|---|
| `<D-v>` | c, i, t | Вставить из буфера обмена |
